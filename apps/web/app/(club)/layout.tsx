import type { ReactNode } from "react";

/**
 * Future club operations route group.
 * Structure only — no club dashboard in Phase 4.
 */
export default function ClubGroupLayout({ children }: { children: ReactNode }) {
  return children;
}
