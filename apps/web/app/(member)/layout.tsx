import type { ReactNode } from "react";

/**
 * Future member experience route group.
 * Structure only — no member features in Phase 4.
 */
export default function MemberGroupLayout({ children }: { children: ReactNode }) {
  return children;
}
