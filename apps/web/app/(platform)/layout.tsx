import type { ReactNode } from "react";

/**
 * Future platform admin route group.
 * Structure only — no admin panel in Phase 4.
 */
export default function PlatformGroupLayout({ children }: { children: ReactNode }) {
  return children;
}
